## Pull Request

### Summary
<!-- What changed, why, and which issue it addresses. -->

- 

### Verification & Testing
- [ ] Image builds cleanly without local dependencies: `docker build -t test-build .`
- [ ] Container launches and serves web UI: `curl -I http://localhost:5000/`
- [ ] Hardware acceleration packages verified (`vainfo`)
- [ ] Base CVE count checked (`docker scout cves` or `trivy`)

```bash
# Output or command log
```
